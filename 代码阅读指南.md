# 自动盾反 1.69 代码阅读指南

这个模组是客户端“攻击预测与按键辅助”。真正的盾反免伤和反击由服务器上的《棱镜》实现。

## 总体流程

```text
进入世界
  ↓
playeractivated 自动调用 autoshield()
  ↓
start() 创建 Shieldthread
  ↓ 每个模拟帧
needshield() 搜索附近实体
  ↓
CheckTargetHitFrame() 查询 prefab + 动画规则
  ↓
检查目标、动画帧、Ping、朝向和距离
  ↓
DoShield() 暂存鼠标物品、装备盾牌、发送 ATTACK_SHIELD_L
```

## 建议阅读顺序

1. `Autoshield.targettable`：理解攻击配置的数据格式，不必逐条阅读。
2. `Autoshield:CheckTargetHitFrame()`：理解一只怪物如何被判定为即将命中。
3. `Autoshield:needshield()`：理解附近实体怎样被收集和遍历。
4. `Autoshield:DoShield()`：理解物品和 RPC 的执行顺序。
5. `Autoshield:start()`、`stop()`、`autoshield()`：理解自动线程和 Z 键开关。
6. 文件末尾：棱镜根须、羽毛、艾剑及其他模组人物的额外兼容功能。

## targettable 字段

| 字段 | 作用 |
|---|---|
| `startframe` | 开始触发的客户端动画帧 |
| `duration` | 允许触发的窗口长度，默认 5 帧 |
| `isaoeatk` | 范围攻击，不强制要求敌人锁定本地玩家 |
| `facingcheck` | 限制敌人必须朝向玩家 |
| `distcompense` | 在客户端攻击距离上增加或减少补偿 |
| `realdist` | 直接指定固定检测距离 |
| `checkpawloop` | 对持续蓄力动作累计帧数 |
| `doshieldfn` | 自定义触发判断 |
| `cancelshieldfn` | 自定义取消判断 |

## 延迟公式

代码使用：

```lua
shielddelay + characterdelay - ping对应的帧数
```

- `shielddelay`：左右方向键手动调节；增加会更晚，减少会更早。
- `characterdelay`：特殊人物动作修正。
- Ping 被减去，所以网络延迟越高，客户端会越早发送动作。

## 快捷键和额外功能

- `Z`：开启或关闭自动盾反。
- `←`：将触发帧调早 1 帧。
- `→`：将触发帧调晚 1 帧。
- `/agronssword`：启动或关闭艾剑搜索线程。
- `darktest`：黑暗测试功能，会反复尝试棱镜举盾动作。

## 阅读时需要注意

- `Sleep(0)` 表示每个模拟帧继续运行，不是完全不等待。
- `SendRPCToServer()` 只是客户端向服务器请求动作，最终是否成功由服务器判断。
- `targettable` 中的 `startframe` 通常是“建议举盾帧”，不一定是真正的伤害发生帧。
- `needshield()` 返回遍历到的第一个满足条件的实体，没有对多个威胁排序。
- 文件末尾还有与核心盾反无关的自动收回羽毛、特殊人物闪避和寻找艾剑功能。
